{
	// database with passwords, logins and tokens
	"auth_db_loc": "/home/basket/web_db/accounts_auth.json",
	// database with user allowance
	"clearance_db": "/home/basket/web_db/ctrl_clearance.json",
	// database with previews and a temp dir
	"preview_db": "/home/basket/pr_db",
	// root of the target file system
	"system_root": "/home/basket/scotland",

	// ffmpeg stuff (required)
	"ffmpeg": ""
}